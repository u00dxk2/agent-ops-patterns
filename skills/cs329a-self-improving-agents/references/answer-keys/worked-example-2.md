# Worked example 2 - a system we do not own

The first worked example is the run on the system this skill was written for. This one is the run on a system nobody here has touched: [NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent), a self-described self-improving agent (238k stars, 10,666 files at the commit read), chosen because the ten ideas are about exactly what it claims to do. Read-only mandate; nothing was written into the target; the record is reproduced below in full, then the adversarial round that corrected it, then what the run said about the skill.

**This is the corrected record.** A cross-family adversarial round (Codex, same day) returned fourteen findings against the first version — five of them P0, including two verdicts that fell. They are folded in below and itemized in "The Codex adversarial round"; the original wrong rows are quoted there, not silently edited away.

**Read this file after your table is filled, not before.** Same warning as the first example: it contains verdicts, and if the repo you were pointed at is this one or shaped like it, it is an answer key.

---

# CS329A disposition - NousResearch/hermes-agent - 2026-08-29

**Run by:** Claude Opus 5 in Claude Code, one session, no sub-agents; adversarially reviewed by Codex (gpt-5-class), findings re-verified and folded the same day · **Operator:** David Kooi · **Time spent:** ~1 h 10 min for the run (of which ~15 min was cloning — Windows path-length failures under `website/i18n/…`, fixed with `core.longpaths`), plus ~45 min for the adversarial round and fold

**Target:** `NousResearch/hermes-agent` at `4209d371aa1bb8840ce8447555bdd863a1a96c38` (main, pushed 2026-08-29T18:04Z), shallow clone into a scratch directory. Every `file:line` below is against that commit; the reviewer confirmed key files hash-identical to it.

**Mandate for this run:** read-only - operator's words: "I agree with your analysis and recommendations please proceed" (2026-08-29), endorsing the proposal "Read-only mandate, clone to scratchpad, one long run." Nothing was written into hermes-agent. This record is delivered into the operator's own repo, as named.

**Prior records found in this repo:** none - from the clone root, `rg -n -i -c "cs329a|self-improving ai agents|stanford cs329" .` · exit 1 · 0 matches. `docs/` has no dispositions directory.

**Sources actually opened this run:**
- `references/ideas.md` - yes
- `references/papers.md` → none fetched
- `references/lectures.md` → not opened
- `references/worked-example.md` → **opened before the run began**, while loading the skill's files to learn the procedure. The target is not the system it describes, so its verdicts do not transfer - but the step-7 ordering was not honored, and this line is where that gets said.
- `references/worked-example-2.md` → did not exist; this record is it.

**Surfaces inventoried** (paths against the commit above; negative searches carry directory · command · exit · matches — all run from the clone root):
- **LLM-as-judge / verifier / grader call sites, and whether the grader is the producer:** `hermes_cli/goals.py:1277` `call_llm(task="goal_judge")` - decides whether a goal is done; routed through the auxiliary chain, whose first resolution is the main model (`agent/auxiliary_client.py:7-9`), so the judge is the worker unless `auxiliary.goal_judge` is set. `evals/compaction/runner.py:60-67` `JUDGE_PROMPT` scores answers against gold in the compaction eval. `agent/background_review.py:1-13` forks the same agent after every turn to judge what to save. `agent/review_engine.py:1-12` `/review` spawns a reviewing subagent that inherits the parent's credentials unless `auxiliary.review` is set. `.coderabbit.yaml:16-18` auto-review disabled, summonable on demand. **And one whole surface the first pass missed because it searched only Python:** the bundled `skills/research/research-paper-writing/SKILL.md` defines a judge PANEL — three blind chain-of-thought judges ranking candidates by Borda count (`:742-748`), a 3-5 reviewer ensemble with deliberate negative bias (`:789-795`), and a meta-reviewer aggregating them. In this ecosystem, markdown-defined agent workflows are code; found by the cross-family reviewer.
- **Candidate generation with selection:** clone root · `rg -n "best_of_n|best_of\b|n_samples|num_samples|num_candidates|majority_vote|self_consistency" --glob "*.py" .` · exit 0 · 4 lines, all audio sample counts (`tools/voice_mode.py:1142-1143`, `tests/tools/test_voice_mode.py:659-660`) — but the search missed the repo's own HYPHENATED vocabulary: `tools/delegate_tool.py:3589-3591` deliberately permits identical-goal fan-outs as "best-of-N / ensemble sampling" (duplicate-goal rejection was removed because it broke real workflows), with orchestrator guidance to synthesize the results. Found by the reviewer. `agent/moa_loop.py` fuses, never selects. The research-paper-writing skill's Autoreason loop generates three candidates per pass (`:742-748`) and its judge panel picks — see idea 2.
- **Eval sets, fixtures, golden corpora; held-out?** Four batteries under `evals/`: `browser_use/` (oracle-checked, `tasks/easy.json` 5, `tasks/hard.json` 6), `compaction/` (LLM-judged recall against generated gold, `results/SCORECARD-2026-08-15.md`), `readtool/` (`results/SUMMARY.md`), `session_search_schema/` (programmatic oracles, "no LLM judging", `README.md:17`; results for 3 models under `results/pr95570/`). Held-out split: clone root · `rg -n -i "holdout|held.out|hold.out set|private test" --glob '!website/**' --glob '!locales/**' .` · exit 0 · matches only in `optional-skills/` (darwinian-evolver's TRAINABLE/HOLDOUT template; mlops reference docs) and `datagen-config-examples/web_research.yaml:46` `eval_size: 25 # Number of held-out questions per eval run` - and `rg -n "eval_size" --glob "*.py" .` · exit 1 · 0 matches: nothing reads that key.
- **Health checks / gates / CI, and where verdicts live:** 30 workflow files in `.github/workflows/` (reviewer recounted: 30); `ci.yaml` is the gate, `tests.yml` is `workflow_call`-only (`tests.yml:3-4`) and is called by it. Verdicts: GitHub Actions history (read below). Cron: a per-attempt ledger implementation, `cron/executions.py:1-5` (terminal states `completed/failed/unknown`, immutable) — code evidence only; no local execution rows were read. Monitor jobs record `no_change` ticks (`cron/monitor.py:8-9`). `hermes doctor` exists (`hermes_cli/doctor.py`); not read.
- **Pre-execution review points:** `tools/approval.py` — dangerous-command detection with an auxiliary-LLM "smart approval" mode: `_smart_approve` at `:3575-3580` calls the model to assess the command, the verdict is applied at `:4927-4952`, and `tools/terminal_tool.py:3233-3246` runs the guard BEFORE command execution. Opt-in: the approval mode defaults to `manual` (`tools/approval.py:3414-3417`). A consecutive-denial circuit breaker exists but is prompt-text escalation, not a hard block — its own comment says nothing stops the model retrying variants (`:2703-2708`). `hermes_cli/goals.py:1660-1672` quality gates run before the LLM judge, with an unchanged-workspace rule that refuses to re-run an identical red suite. `hermes_cli/kanban_decompose.py:1-12` an auxiliary LLM produces a task graph that is created atomically - no review step between plan and children.
- **Research / reference ingestion:** `tools/web_tools.py:1048-1062` `web_extract` returns clean page text "with NO LLM summarization", head+tail truncated at `DEFAULT_EXTRACT_CHAR_LIMIT = 15000` (`:635`). `agent/auxiliary_client.py:3-4` lists "web extraction" as an auxiliary task. `skills/research/grounded-citations/SKILL.md:17-25` a bundled skill: ledger-numbered citations, verbatim quotes rejected unless present in fetched text, `[unverified]` flag, `verify --evidence`.
- **Multi-step unattended dispatch:** cron (`cron/scheduler.py`, inactivity limit 600 s, no wall-clock cap, `cron/jobs.py:204-209` — the one allowance here that is a DURATION), goals (`hermes_cli/goals.py:52` `DEFAULT_MAX_TURNS = 20`), subagents (`agent/iteration_budget.py:5-6` parent 500 iterations, subagent 50 — budgets, not durations), `batch_runner.py` (trajectory generation over datasets; saves a trajectory ONLY when the run succeeded, `:452-453`, and discards zero-reasoning samples, `:454-457`), kanban swarm (`hermes_cli/kanban_swarm.py`, not read).
- **Model choice per call site:** `agent/auxiliary_client.py:7-15` one resolution chain for every side task, main model first; per-task overrides `:36-38`; `auxiliary.background_review.{provider,model}` is its own seam (`agent/background_review.py:325-331`) defaulting to the parent's live runtime; MoA has its own default slots (`hermes_cli/moa_config.py:14-22`). Aux usage is accounted per session (`agent/aux_accounting.py:1-11`).
- **Surfaces I could not reach:** the `hermes-agent-dev` skill. `agent/background_review.py:15` and `agent/system_prompt.py:5-7` point to its `references/self-improvement-loop.md` for the learning loop's invariants; `AGENTS.md:1096-1097` points to its `references/new-skill-pr-salvage.md`. Clone root · `rg -n "self-improvement-loop|hermes-agent-dev" .` · exit 0 · ~50 references, no directory; it installs from the skills hub as `github/hermes-agent-dev` (`tools/terminal_tool.py:3432`). **UNREADABLE** from the tree. Also not reached: anything at runtime — no install, no logs, no local databases; `cli.py` (1.0 MB), `run_agent.py` (434 KB), `hermes_state.py` (697 KB) - read only at grep hits; `website/` and `locales/` excluded from every search except where noted.

**Bars pre-committed before any deciding number was read:**
- **4 (CI):** over the last ≥30 completed `main` runs of `ci.yaml`, ≥98% one conclusion = the check carries no signal → APPLIES; mixed = signal, and the history is the record → ALREADY IN PLACE (running); <30 runs → NOT DECIDABLE. Written before `gh run list` was called.
- **4 (eval batteries):** a battery whose every arm sits at ceiling is saturated. **Written after its number** - the browser battery's 18/18 was in a README opened during the inventory. Counted below.
- **6:** a default unattended allowance ≥15 min puts the dispatch in the 50% regime → APPLIES unless abandonment and repetition are detected as counted failure classes. Written before the cron/goal defaults were read. (The adversarial round narrowed what can carry this: only cron's allowance is a duration; iteration and turn budgets are not time.)
- **2:** any judge ensemble needs ≥100 labeled rows joinable to per-judge verdicts to be ALREADY IN PLACE.

## Verdicts

| # | Idea | Verdict | Evidence | Action / next step / declined because |
|---|---|---|---|---|
| 1 | Coverage vs. selection | **DOES NOT APPLY** (narrowed) | No ENFORCED N→1 selector in code. Clone root · `rg -n "best_of_n\|best_of\b\|n_samples\|num_samples\|num_candidates\|majority_vote\|self_consistency" --glob "*.py" .` · exit 0 · 4 lines, all audio sample counts — a search the adversarial round showed misses the repo's own hyphenated vocabulary: identical-goal fan-outs are a sanctioned "best-of-N / ensemble sampling" pattern (`tools/delegate_tool.py:3589-3591`), each task returning one result (`:3659`) with orchestrator guidance to SYNTHESIZE (`:1270-1272`) — fusion's shape, not selection's. When a user runs that pattern, whatever picks is the parent model ad hoc: the same-model-ranks configuration idea 1 itself routes to ideas 2 and 8. MoA fuses (`agent/moa_loop.py:1330-1338`). | Declined, narrowly: no selector exists whose accuracy could be measured. If a selection step is ever added to duplicate-goal delegation, idea 1's question (selector accuracy vs. labels) activates as written. |
| 2 | Filter verifiers before ensembling | **NOT DECIDABLE** | An ensemble EXISTS — markdown-defined, missed by the first pass's Python-only search, found by the cross-family reviewer: `skills/research/research-paper-writing/SKILL.md:742-748` (three blind CoT judges rank candidate drafts by Borda count, k=2 convergence), `:789-795` (3-5 independent reviewers, different models or temperatures, deliberate negative bias), plus a meta-reviewer aggregating them. No labels: nothing joins those judges' verdicts to outcomes, no judge has ever been dropped for failing a floor, and the ensemble votes unweighted. Per the pre-committed bar (≥100 joinable rows) and idea 2's own precedence, the verdict is the missing record, not the gap. Elsewhere: two lone Python judges (`hermes_cli/goals.py:1277`; `evals/compaction/runner.py:60-67`), each routed to ideas 3 and 8. | Record that would decide it: persist per-judge verdicts beside an outcome label (did the Borda winner survive human editing? did the simulated reviews match real reviewer complaints?) — one line per judge per pass. Not started - read-only. |
| 3 | Meta-verification | **NOT DECIDABLE** | Two model-reviews-model loops exist: `/review` (`agent/review_engine.py:1-8`, an independent subagent reviews a PR/diff and its result re-enters the session) and CodeRabbit on demand (`.coderabbit.yaml:9-12`). No record of which findings were real: clone root · `rg -i --count-matches --include-zero "confirmed\|refuted\|false.positive\|precision" agent/review_engine.py agent/background_review.py` · exit 1 · `review_engine.py:0`, `background_review.py:0`. The only gap is the missing record → NOT DECIDABLE by precedence. | Record that decides it: one line per `/review` finding - confirmed or refuted - written when the primary agent acts on it. How long it would take to matter is NOT estimable from this tree: `/review` invocation counts live off-repo, and CI run volume says nothing about review findings (the first version estimated from it; the adversarial round struck that). Not started - read-only. |
| 4 | Signal needs spread | **ALREADY IN PLACE (running — CI only)** | `gh run list --repo NousResearch/hermes-agent --workflow ci.yaml --branch main --status completed --limit 100`: **78 success / 6 failure / 14 cancelled / 2 action_required**, 2026-08-28T11:58Z → 08-29T18:40Z. Bar not met (no conclusion ≥98%) - the check carries signal and Actions history is the per-run record. The (running) tag rests on that query ALONE — the network read this run's mandate allowed. Cron's per-attempt ledger (`cron/executions.py:1-5`) and monitor `no_change` ticks (`cron/monitor.py:8-9`) are code evidence that a record WOULD exist on a running install; no rows were read. Evals store per-cell results (`evals/*/results/`). Bar written after its number: the browser hard battery reads 18/18 on every arm (`evals/browser_use/README.md:58-64`) - but that battery's declared metric is tokens/calls **at accuracy parity** (`README.md:5-7`), so the ceiling is its control, not its signal; compaction (40.0-96.7 recall, `SCORECARD-2026-08-15.md:12-16`) and session_search (49/54 vs 52/54, `README.md:62-67`) have spread. | None required. If the browser battery is ever cited for accuracy rather than cost, it needs harder tasks first. |
| 5a | Judge the plan before execution | **ALREADY IN PLACE (code, opt-in)** | A model judges the command before the tool answers — in smart mode: `_smart_approve` (`tools/approval.py:3575-3580`) asks the auxiliary LLM to approve/deny/escalate; the verdict is applied at `:4927-4952`; `tools/terminal_tool.py:3233-3246` runs the guard before execution. Opt-in: approval mode defaults to `manual` (`:3414-3417`). The consecutive-denial breaker is prompt-text escalation, not a block — its own comment says nothing stops variant retries (`:2703-2708`). Gates precede the goal judge with a no-rerun-on-unchanged-workspace rule (`hermes_cli/goals.py:1660-1672`). Failed-but-good-steps: **the training pipeline keeps only successes** — `batch_runner.py:452-453` saves a trajectory only when `result["success"]`, and discards zero-reasoning samples (`:454-457`); SWiRL's finding is that outcome-only filtering teaches the model what it could already do. | Smallest step: keep failed trajectories whose steps were judged good — the process-filtered negative half SWiRL trained on — behind a flag in `batch_runner.py`. (running) not earned: no approval log in the tree. |
| 5b | Hold out a private test set | **APPLIES** | The batteries that gate prompt/schema changes are the sets the change is tuned on: the session_search schema diet was iterated and reported on the same six tasks (`evals/session_search_schema/README.md:16-27, 57-67`); compaction question banks were "regenerated in the 4-way run" (`SCORECARD-2026-08-15.md:18-20`). Negative search for a split: clone root · `rg -n -i "holdout\|held.out\|hold.out set\|private test" --glob '!website/**' --glob '!locales/**' .` · exit 0 · hits only in `optional-skills/` and `datagen-config-examples/web_research.yaml:46` (`eval_size: 25 # Number of held-out questions per eval run`), and `rg -n "eval_size" --glob "*.py" .` · exit 1 · 0 matches - the one held-out setting in the core repo has no consumer. | Smallest step: split each task bank (e.g. 4 tuned / 2 held of the six session_search tasks), report both halves with the same denominators; wire `eval_size` or delete the key. |
| 6 | Reliability horizon | **APPLIES** | The duration-bearing allowance: cron jobs have a 600 s **inactivity** limit and no wall-clock cap (`cron/jobs.py:204-209`) — a job that keeps producing output runs indefinitely, so scheduled unattended work sits in the 50% regime by design. Budget context (counts, not time — the adversarial round's correction): parent 500 iterations / subagent 50 (`agent/iteration_budget.py:5, 21`), goals 20 turns (`hermes_cli/goals.py:52`); no runtime durations were read, so no regime claim rests on them. Failure classes: repetition is detected for output loops (`agent/repetition_guard.py:1-18`, #86581) and tool-call loops (`agent/tool_guardrails.py:1-7`); abandonment only for coding edits (`agent/verification_stop.py:1-5`: finishing after an edit with no fresh evidence → bounded nudge, max 3 per `agent/verify_hooks.py:21`). Context at start: coding posture injects a workspace snapshot and brief (`agent/coding_context.py:16-17`), `AGENTS.md` discovered under cwd (`agent/system_prompt.py:18`). Nothing checks that a reference the agent was told to read was read; `agent/prompt_builder.py:589` asks the model to re-fetch on disagreement - the prompt exhortation the course argues against. | Smallest step: count "halted on repetition" and "finished without evidence" as failure classes beside `failed` in `cron/executions.db`, so the 50%-vs-80% question becomes a number instead of a default. |
| 7 | Deep research has a low ceiling | **ALREADY IN PLACE (code, opt-in)** | `skills/research/grounded-citations/SKILL.md:17-25`: citations come from a retrieval ledger, verbatim quotes are "rejected unless they literally appear in the fetched page text", model-knowledge claims are flagged `[unverified]`, `verify --evidence` fails a draft whose sources carry no evidence. Bundled (`skills/`, not `optional-skills/`), invoked when relevant, not on by default. Retrieval layer: `web_extract` dumps clean page text, truncated at 15,000 chars, no LLM step (`tools/web_tools.py:1056-1062, 635`) - Search-o1 would extract the span; LLM extraction is an opt-in auxiliary task (`agent/auxiliary_client.py:3-4`). | (running) not earned: no usage counter for this skill was found in the tree; `tools/skill_usage.py:3-5` counts skill views off-repo, so the number may exist on a running install. Next: read it. |
| 8 | Diversity is the bottleneck | **APPLIES** | The learning loop's judge is the generator, at every seam: `agent/background_review.py:266-296` runs by default after turns and `:325-331` defaults its fork to the parent's live runtime unless `auxiliary.background_review.{provider,model}` names a different model; the curator runs on the auxiliary chain (`agent/curator.py:3, 19`), whose first resolution is the main model (`agent/auxiliary_client.py:7-9`); `/review` inherits the parent's credentials unless `auxiliary.review` is set (`agent/review_engine.py:10-12`); the goal judge routes the same way (`hermes_cli/goals.py:1274-1278`). A second family is defaulted for MoA only (`hermes_cli/moa_config.py:14-22`: gpt-5.5 and deepseek-v4-pro advise, claude-opus-4.8 aggregates) — which proves the multi-provider plumbing, not that any auxiliary default uses it. Diversity of generated skills/memories: not measured (no term found; not exhaustively searched). | Smallest step: default `auxiliary.background_review`, `auxiliary.review`, and `auxiliary.goal_judge` (and evaluate `auxiliary.curator`) to a different family than `model` when one is configured. Each seam already exists; only the defaults change. |
| 9a | Fuse the top-k instead of picking one | **ALREADY IN PLACE (code)** | `agent/moa_loop.py:1-6` `/moa` gathers reference-model outputs before each iteration; parallel fan-out at `:1239-1252` with results collected at `:1274-1286`; the aggregator SYNTHESIZES — the synthesis prompt at `:1330-1338` instructs it to merge the reference responses into guidance, disagreements included, never to pick one. (The first version cited the advisor-prompt and reasoning-config regions; corrected by the adversarial round.) Archon's shape. Not measured: `ls evals` → `browser_use compaction readtool session_search_schema`; no MoA arm. | Next: one A/B cell of `/moa` against single-model on an existing oracle-checked battery. |
| 9b | Process reward catches right-answer-wrong-process | **APPLIES** | What exists is verification-SCOPE bookkeeping, not a process check: `agent/verification_evidence.py:1-6` records which checks ran and refuses to upgrade `targeted` to `full` (`:553, :591-620`) — but `status` is `passed` purely from `exit_code == 0` (`:549-555`), and nothing anywhere assesses whether a green final output was reached through a wrong intermediate step. The first version called this ALREADY IN PLACE; the adversarial round is right that scope honesty is orthogonal to process reward. | Smallest step: the evidence ledger already stores per-command rows — add a field for what step the command verified (plan-step id or edit hash), so a later pass can ask "which green outcomes had unverified intermediate steps". |
| 10 | Route by difficulty; small models carry the bulk | **APPLIES** | Model choice per side task is a default, and the default is the main model (`agent/auxiliary_client.py:7-9`, overrides `:36-38`). Classification-shaped sites on that chain: title generation, compression, session-search summaries, `cron/scripts/classify_items.py`, `hermes_cli/kanban_decompose.py:5`, the goal judge. The measurement exists: `agent/aux_accounting.py:1-11` records aux token usage per session (#23270). Disagreement as a difficulty signal: `git grep -l -i disagree HEAD -- "*.py"` · 69 files; the `agent/` hits are config/identity mismatches (`bedrock_adapter.py:561`, `credential_pool.py:2128`, `context_compressor.py:6082`) or a prompt line (`moa_loop.py:1334`); no sampling-disagreement router. | Smallest step: read the aux-spend share from the accounting the repo already keeps and set a small-model default for the classification-shaped tasks; leave generation on the main model. |

## Summary

Verdicts (12):
- **APPLIES: 5** - 5b (evals tuned and reported on the same tasks; the one held-out setting has no reader), 6 (cron unattended runs unbounded in wall-clock; abandonment counted only for coding edits), 8 (every learning-loop judge defaults to the generator; a second family is defaulted for MoA only), 9b (verification-scope bookkeeping exists; nothing catches right-answer-wrong-process), 10 (side tasks default to the main model; the spend is already accounted).
- **DOES NOT APPLY: 1** - 1 (no enforced N→1 selector; the sanctioned duplicate-goal pattern is guided toward synthesis, and any ad-hoc picking is the same-model configuration ideas 2/8 own).
- **ALREADY IN PLACE (code): 3** - 5a (`tools/approval.py:3575-3580` smart approval judges the command pre-execution; opt-in), 7 (`skills/research/grounded-citations/SKILL.md:17-25`; opt-in), 9a (`agent/moa_loop.py:1330-1338`).
- **ALREADY IN PLACE (running): 1** - 4 (CI only: `ci.yaml` on `main`, 78/6/14/2 over 100 runs in 31 h; cron's ledger is code evidence).
- **NOT DECIDABLE: 2** - 2 (a real judge panel + reviewer ensemble, markdown-defined, with zero joinable labels; the record is the next step), 3 (two review loops, zero confirmed/refuted rows; the record is the next step; not started).
- **UNREADABLE: 0** ideas — but **surfaces unreachable: 1** (the hub-installed `hermes-agent-dev` skill holding the learning-loop invariants); no verdict turned on it alone.

Actions:
- **Acted on: 0** - mandate was read-only. Writing this record does not count.

Integrity:
- **Bars written after their number: 1** - idea 4, the browser battery ceiling. Disclosed in the bars section and the row.
- **Highest-leverage take, if only one thing gets done:** idea 8 - the routing seams for `auxiliary.background_review`, `auxiliary.review`, and `auxiliary.goal_judge` all exist and all default to the generator; pointing their defaults at a second family when one is configured moves the entire self-improvement loop off the configuration the course rates weakest without new logic. (The first version named two of the three seams and missed the one on the loop it cited most; the adversarial round closed that.)
- **What this run did NOT check:** the `hermes-agent-dev` skill and its self-improvement-loop invariants (off-repo, hub-installed); any runtime evidence other than the GitHub Actions history query — no install, no logs, no local databases, so every (code) verdict is code-only and cron's ledger is unproven in motion; `cli.py`, `run_agent.py`, `hermes_state.py` beyond grep hits (1.0 MB, 434 KB, 697 KB); `hermes_cli/kanban_swarm.py`; whether the compaction judge is the same model as the answerer (`evals/compaction/policies.py:16` sets one `EVAL_MODEL` for the compressor; the judge's `_call` was not traced); `website/` and `locales/` except where a search says otherwise; no paper was opened; the worked example was read before the run, as stated above.
- **Refuted by:** a same-day cross-family adversarial round — fourteen findings, five P0, two verdicts overturned; all folded above and itemized below. Nobody has refuted the CORRECTED record yet; someone should before it is acted on, and the first version's fate says why.

---

## The Codex adversarial round (2026-08-29, same day)

A second model family was pointed at the first version of this record with the clone, the skill, and the template, and told to attack every class of fabrication and overclaim. It returned **fourteen findings — 5 P0, 6 P1, 3 P2 — every one re-verified against the tree before folding; twelve held as written, two were adapted.** What fell:

- **Two verdicts were wrong.** Idea 2's DOES NOT APPLY missed a real judge ensemble because the inventory searched only Python — `research-paper-writing/SKILL.md` defines a three-judge Borda panel and a 3-5 reviewer ensemble in markdown, and the record had already accepted a markdown skill as real functionality for idea 7, so it could not consistently exclude this one (now NOT DECIDABLE: ensemble, zero labels). Idea 9b mistook scope bookkeeping for process verification — `status` comes from `exit_code == 0` and nothing judges intermediate steps (now APPLIES).
- **One claim asserted the opposite of the code.** "Saves every trajectory with per-tool success/failure counts" — `batch_runner.py:452-453` saves only successes and discards zero-reasoning samples, and the record's own not-checked line admitted the file was never read past its header. The claim came from the header. It is now the 5a row's smallest next step, which is where it always belonged.
- **Two citation sets pointed at the wrong lines.** 5a's cited `:141-183` were observer hooks, not the LLM judgment (real path: `:3575-3580`, `:4927-4952`, pre-execution at `terminal_tool.py:3233-3246` — the verdict survived, relabeled opt-in); 9a's cited regions were an advisor prompt and reasoning config, not aggregation (real synthesis: `:1330-1338`).
- **The record's own most-likely-wrong nomination was wrong.** It named 5a; 5a survived with corrected citations, while 2, 9b, and the batch-runner claim fell. Self-assessment of which row is weakest is itself a same-model judgment — idea 8 in miniature, inside the record about idea 8.
- **Smaller but real:** idea 1's search missed the repo's hyphenated "best-of-N" vocabulary (`delegate_tool.py:3589-3591` — verdict narrowed, adapted from the reviewer's wording); idea 3 estimated a fill rate from CI volume, which measures commits, not review findings (struck); idea 6 classified iteration budgets into a time regime (only cron's allowance is a duration); idea 4 bundled cron's code-only ledger under a (running) tag earned solely by the CI query; three zero-match search receipts omitted directory and exit code (rg exits 1 on zero matches — all restated in full form); the disagree count was 71 under one tool and is 69 under the reproducible `git grep -l` (adapted: restated under the latter, with the command); the sources list omitted the row for this file itself; and one skill-feedback note overclaimed that reading the references table first is inevitable rather than what this run did.
- **What it could not check, kept honest:** the CI query (network), the hub skill, all runtime state, and every claim about the run's own process.

The round's lesson matches the first worked example's: the record had been written carefully, checked its own arithmetic, and disclosed its own weakest row — and a different model family still overturned two verdicts in forty minutes. "Refuted by: nobody yet" is not a formality.

---

## What the run said about the skill

Ten things, for the next revision. None survived the adversarial round unchanged in status: the round confirmed some, corrected one, and added weight to others. Notes are as originally written except where marked.

- **The negative-search form caught a wrong verdict before it was written — and its absence let one through.** A keyword sweep for judges returned 267 hits across 69 files - `code_verifier`, `skillevaluator` - and read as "no LLM judges." Writing the DOES NOT APPLY row required the exact command, and re-running it at phrase level found two judges the sweep had buried. But the sweep was Python-only, and the adversarial round found a whole judge ensemble in a markdown skill the same discipline should have caught: the evidence form works only as wide as the search's file types. Keep the rule; add to it that **an inventory's glob is part of the receipt**.
- **Step 7 cannot be relied on when loading the skill means reading its file list.** This run opened the worked example while learning the procedure and said so; the adversarial round corrected the stronger claim that this is inevitable (the references table sits at the end of SKILL.md, after the gating instruction — a careful reader can comply). Move the answer keys into `references/answer-keys/` with their own README so the failure requires effort instead of preventing it.
- **"Bars before numbers" has a hole for numbers that arrive in prose.** The browser battery's 18/18 was in a README opened for the *inventory* - the step where reading numbers is allowed - and the bar it should have preceded was only obvious after seeing it. Disclosure handled it (count: 1). Worth one sentence in step 3: "a number you meet while inventorying counts as read."
- **(code)/(running) is missing a third state: (installed, opt-in).** Smart approval (5a) and grounded-citations (7) both exist, both are off the default path. This record now writes "(code, opt-in)" as row prose; whether it becomes a counted tag is a template decision.
- **UNREADABLE for a dependency outside the repo worked, but the count hid it.** The template's UNREADABLE line counts ideas, so it read 0 while a real unreachable surface sat above it. This record adds "surfaces unreachable: N" beside it; the template should too.
- **A 10k-file repo is inventoried by search, not by reading,** and the `file:line` evidence bar was satisfiable that way — but the adversarial round showed the cost: two citation sets landed on grep hits NEAR the mechanism instead of the mechanism, and a header was quoted for behavior the body contradicts. The discipline that survives: cite what you have READ, and when a claim is about what code *does*, the receipt is the doing line, not the docstring.
- **The eight inventory categories mapped onto a foreign repo with one systematic blind spot:** markdown-defined agent workflows. In a skills-based ecosystem, prompts are code; a Python-only glob inventories half the system. Add to step 2: search prompt/skill directories with the same categories.
- **Idea 2's row needs a home for single judges.** The idea is about ensembles; the repo ALSO has two lone judges with unrecorded precision. Route explicitly in the idea text: "a lone judge with no label record → see 3; a lone judge that is the producer → see 8."
- **Idea 6's bar was easy to write and easy to over-apply.** The first version put iteration budgets into a time regime; only cron's allowance is a duration. The useful content is the failure-class inventory. Rather than splitting the idea, make its verdict turn on whether abandonment and repetition are *counted* failure classes, with the horizon regime as context.
- **"Time spent" should include the clone and the round.** Fifteen of seventy minutes went to Windows path-length errors; the adversarial round and fold cost another ~45. Both are real costs of running this skill on a large repo, and the next reader should budget them.

**Its own caveat, revised by events:** one model family wrote this record; a second overturned two of its twelve verdicts and corrected five citation sets the same day. Two-thirds of the surviving verdicts still rest on docstrings plus bounded reads, now with the doing-line rule above. The corrected record has not been adversarially read.
